# 🧩 Структура и архитектура проекта

Проект построен на **React + TypeScript** с использованием **Redux Toolkit** и **React Router v6**.  
Основная точка входа — `main.tsx`, в котором подключаются все провайдеры и инициализируется приложение.  
Компоненты структурированы по слоям (`layouts`, `pages`, `components`, `stores`, `hooks`, `utils` и т.д.) для удобства поддержки.

---

## ⚙️ Точка входа приложения

### 🗂 `src/main.tsx`

Файл, с которого начинается выполнение приложения.

**Назначение:**

- Инициализация React-приложения через `createRoot`.
- Подключение провайдеров окружения:
    - `StrictMode` — проверка потенциальных ошибок на этапе разработки.
    - `BrowserRouter` — маршрутизация страниц (React Router).
    - `Provider` — передача Redux-хранилища (`store`) во всё приложение.
- Импорт `App.tsx` — корневого компонента приложения.
- Импорт `virtual:svg-icons-register` — регистрация SVG-иконок через Vite-плагин.

**Ключевой код:**

createRoot(document.getElementById('root')!).render(
<StrictMode>
<BrowserRouter>
<Provider store={store}>
<App />
</Provider>
</BrowserRouter>
</StrictMode>
);

> > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > >

## 🏗️ Layouts

Папка `src/layouts/` содержит все компоненты, отвечающие за **структуру и макет приложения** — шапку, подвал, основное содержимое, а также общий Layout, объединяющий всё вместе.

---

### 📁 `AppLayout/`

**Главный каркас приложения.**  
Используется для оборачивания всех страниц (`Home`, `MasterArea` и др.), включает:

- `Header` — верхнюю панель сайта;
- `Footer` — нижнюю часть страницы;
- `FooterNav`, `FooterLegal`, `SupportInfo` — дополнительные блоки футера;
- `ScrollButton` — кнопка “наверх/вниз”.

> 📍 Использует компонент `Logo`, `Dropdown`, `LinkButton`, `CountrySelector`, `IconSprite` и др.

**Расположение:**  
`src/layouts/AppLayout/index.tsx`

---

### 🧭 `Header/`

Компонент шапки сайта.  
Содержит навигацию, логотип, выпадающие меню и кнопки входа.  
Можно переиспользовать как самостоятельный layout для страниц с разной структурой.

**Расположение:**  
`src/layouts/Header/index.tsx`

---

### 🦶 `Footer/`

Базовый контейнер для нижней части страницы.  
Используется в составе `AppLayout` и может принимать дочерние элементы (`FooterNav`, `FooterLegal` и т.п.).

**Расположение:**  
`src/layouts/Footer/index.tsx`

---

### 📚 `FooterNav/`

Блок с навигацией в подвале (разделы “Клиент”, “Мастер”, “О нас”).  
Включает ссылки через `LocalLink` и компонент `SupportInfo`.

**Расположение:**  
`src/layouts/FooterNav/index.tsx`

---

### ⚖️ `FooterLegal/`

Юридическая информация и копирайт компании.  
Содержит описание компании, годы работы, вид деятельности, возрастные ограничения.

**Расположение:**  
`src/layouts/FooterLegal/index.tsx`

---

### 💬 `SupportInfo/`

Контактная информация службы поддержки.  
Содержит:

- адрес почты (по умолчанию `support@dvesoroki.by`);
- время работы;
- ссылки на социальные сети (YouTube, VK).

**Расположение:**  
`src/layouts/SupportInfo/index.tsx`

---

### 🧱 `Main/`

Контейнер для основного содержимого страницы.  
Используется внутри `AppLayout` или `MasterArea`.

**Расположение:**  
`src/layouts/Main/index.tsx`

---

### 🧩 `index.ts`

Файл-агрегатор, который экспортирует все layout-компоненты:

export _ from './AppLayout';
export _ from './Header';
export _ from './Footer';
export _ from './FooterNav';
export _ from './FooterLegal';
export _ from './SupportInfo';
export \* from './Main';

> > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > > >

## 📄 Pages

Папка `src/pages/` содержит **основные страницы приложения**, каждая из которых представляет собой  
независимый экран (view) и формируется из компонентов (`components`) и общих макетов (`layouts`).

---

### 🏠 `Home/`

Главная страница приложения.

**Назначение:**

- Центральная точка для взаимодействия пользователя с сервисом.
- Демонстрирует основные возможности приложения: поиск мастеров, просмотр карточек, баннеры и акции.
- Использует комбинированную структуру с несколькими секциями, каждая из которых реализована отдельным компонентом.

**Структура страницы:**

- `Header` — шапка с логотипом, меню, выпадающим списком и кнопкой входа.
- `BannerContainer` — верхний баннер с призывом к действию.
- `SearchPanel` — поиск мастеров по фильтрам.
- `MastersToolbar` — переключатель “Списком / На карте”.
- `MasterList` — список мастеров.
- `Slider` — слайдер с акциями и скидками.
- `JoinSection` — секция с предложением присоединиться к сервису.
- `HelpSection` — блок обратной связи.
- `Footer` — нижняя часть страницы с навигацией, юридической информацией и выбором страны.

**Используемые зависимости:**

- Из `components`:  
  `Dropdown`, `Logo`, `Menu`, `SearchPanel`, `MastersToolbar`, `MasterList`,  
  `Slider`, `JoinSection`, `HelpSection`, `IconSprite`, `CountrySelector`, `BannerContainer`, `LinkButton`.
- Из `layouts`:  
  `Header`, `Main`, `Footer`, `FooterNav`, `FooterLegal`.

**Расположение:**  
`src/pages/Home/index.tsx`

---

### 💼 `MasterArea/`

Страница личного кабинета мастера (панель управления).

**Назначение:**

- Отображает профиль мастера, расписание, образование, опыт, портфолио и адреса.
- Использует `AppLayout` для оборачивания контента в общий шаблон с `Header` и `Footer`.
- Подключена к Redux через `useAppSelector` (получение данных из `state.master`).

**Структура страницы:**

- `MasterSidebar` — боковое меню с навигацией по профилю.
- `MasterProfileContainer` — базовая информация мастера (имя, контакты, аватар).
- `ScheduleContainer` — график работы.
- `MasterAboutContainer` — секция “О себе”.
- `MasterEducationContainer` — курсы и сертификаты.
- `MasterExperienceContainer` — опыт работы.
- `MasterAddressesContainer` — адреса салонов / точек приёма.
- `MasterServicesContainer` — предоставляемые услуги.
- `Diplomas` — загруженные дипломы.
- `MasterPortfolio` — галерея портфолио.

**Используемые зависимости:**

- Из `components`:  
  `MasterSidebar`, `MasterProfileContainer`, `ScheduleContainer`, `MasterAboutContainer`,  
  `MasterEducationContainer`, `MasterExperienceContainer`, `MasterAddressesContainer`,  
  `MasterServicesContainer`, `Diplomas`, `MasterPortfolio`.
- Из `layouts`:  
  `AppLayout`, `Main`.

**Расположение:**  
`src/pages/MasterArea/index.tsx`

---

### 🧩 `index.ts`

Файл-агрегатор, экспортирующий все страницы приложения:

export _ from './Home';
export _ from './MasterArea';
